# ===========================================================
# Tourenplan Backend - Beispiel-Umgebungsvariablen
# ===========================================================

# Render fügt diese meist automatisch hinzu (Postgres-Dienst):
DATABASE_URL=postgresql://user:password@host:5432/tourenplan

# Port (auf Render 10000)
PORT=10000

# JWT-Secret für Login
JWT_SECRET=tourenplan_secret

# Optional
NODE_ENV=production
